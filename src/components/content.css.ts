import { style } from '@vanilla-extract/css';

export const imageItemStyle = style({
  width: '130px',
  height: 'auto',
});

export const fullscreenImageItemStyle = style({
  width: '400px',
  maxWidth: '100%',
  height: 'auto',
});

export const reflectionLayerCard02FullscreenStyle = style({
  borderRadius: '24px',
});

export const reflectionLayerCard03InlineStyle = style({
  borderRadius: '8px',
});

export const reflectionLayerCard03FullscreenStyle = style({
  borderRadius: '26px',
});
